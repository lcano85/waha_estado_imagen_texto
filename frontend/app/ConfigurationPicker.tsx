export default function ConfigurationPicker({configurations,value,onChange}:{configurations:any[];value:number[];onChange:(ids:number[])=>void}){const toggle=(id:number)=>onChange(value.includes(id)?value.filter(x=>x!==id):[...value,id]);return <fieldset className="configurationPicker"><legend>Configuraciones</legend>{configurations.map(c=><label key={c.id}><input type="checkbox" checked={value.includes(c.id)} onChange={()=>toggle(c.id)}/><i style={{background:c.color}}/>{c.name}</label>)}{!value.length&&<small>Selecciona al menos una configuración.</small>}</fieldset>}
