type Props={data:{promotions:any[];schedules:any[];configurations:any[];logs:any[]}};
const days=['Lunes','Martes','Miércoles','Jueves','Viernes','Sábado'];
export default function LogTable({data}:Props){const promo=(id:number)=>data.promotions.find(p=>p.id===id),cfg=(id:number)=>data.configurations.find(c=>c.id===id);return <div className="activityGrid"><div><h3>Últimos envíos</h3><div className="logList">{data.logs.length===0&&<p>Sin envíos registrados.</p>}{data.logs.map(l=>{const p=promo(l.promotionId),c=p&&cfg(p.configurationId);return <article key={l.id}><i style={{background:c?.color}}/><div><b>{p?.name||`Promoción #${l.promotionId}`}</b><small>{c?.name} · {l.trigger==='CRON'?'Automático':'Manual'} · {new Date(l.createdAt).toLocaleString('es-PE')}</small></div><span className={`status ${l.status.toLowerCase()}`}>{l.status==='SENT'?'Enviado':l.status==='FAILED'?'Falló':'En proceso'}</span></article>})}</div></div><div><h3>Crons programados</h3><div className="logList">{data.schedules.map(s=>{const p=promo(s.promotionId),c=p&&cfg(p.configurationId);return <article key={s.id}><i style={{background:c?.color}}/><div><b>{p?.name}</b><small>{c?.name} · {days[s.dayOfWeek-1]} a las {s.sendTime.slice(0,5)}</small></div><span className="status scheduled">Activo</span></article>})}</div></div></div>}
