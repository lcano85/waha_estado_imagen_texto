const days=['Lun','Mar','Mié','Jue','Vie','Sáb','Dom'];
export default function DayPicker({value,onChange}:{value:number[];onChange:(days:number[])=>void}){const toggle=(day:number)=>onChange(value.includes(day)?value.filter(x=>x!==day):[...value,day].sort());const all=value.length===7;return <div className="dayPicker"><div><b>Días de envío</b><button type="button" onClick={()=>onChange(all?[]:[1,2,3,4,5,6,7])}>{all?'Quitar todos':'Seleccionar todos'}</button></div><section>{days.map((name,i)=><label key={name} className={value.includes(i+1)?'selected':''}><input type="checkbox" checked={value.includes(i+1)} onChange={()=>toggle(i+1)}/><span>{name}</span></label>)}</section>{!value.length&&<small>Selecciona al menos un día.</small>}</div>}
